import fs from 'node:fs';import path from 'node:path';import {createHash} from 'node:crypto';import {execFileSync} from 'node:child_process';import {fileURLToPath} from 'node:url';
const bundle=path.dirname(fileURLToPath(import.meta.url)),manifest=JSON.parse(fs.readFileSync(path.join(bundle,'manifest.json'),'utf8')),hash=value=>createHash('sha256').update(value).digest('hex');
const run=(command,args,options={})=>execFileSync(command,args,{encoding:'utf8',stdio:['ignore','pipe','pipe'],timeout:180000,...options});
const marker='/opt/lilly-migration/staging/PRODUCTION_ACTIVE',current='/var/www/lilly-platform/current',releaseRoot=`/opt/lilly-security-release/${manifest.version}`,site=`/var/www/lilly-platform/releases/${manifest.version}`,result={version:manifest.version,checks:[]};
const check=(name,passed)=>{result.checks.push({name,passed:!!passed});if(!passed)throw new Error(`Check failed: ${name}`);};
const switchSite=target=>{const next=`${current}.security-next`;if(fs.existsSync(next))fs.unlinkSync(next);fs.symlinkSync(target,next);fs.renameSync(next,current);};
if(process.platform!=='linux'||process.getuid()!==0||!fs.existsSync(marker))throw new Error('Confirmed Alibaba production host/root required');if(fs.existsSync(releaseRoot))throw new Error('Immutable release already exists');
const previous=fs.realpathSync(current);fs.mkdirSync(path.join(releaseRoot,'before'),{recursive:true,mode:0o700});fs.writeFileSync(path.join(releaseRoot,'before/frontend-target.txt'),previous,{mode:0o600});
const oldDefinition=run('docker',['exec','lilly-stage-db','psql','-U','postgres','-d','postgres','-X','-Atc',"select pg_get_functiondef('public.is_allowed_staff()'::regprocedure)"]);fs.writeFileSync(path.join(releaseRoot,'before/is_allowed_staff.sql'),`${oldDefinition.trim()};\n`,{mode:0o600});
run('systemctl',['start','lilly-platform-backup.service']);check('pre-release encrypted backup',run('systemctl',['show','lilly-platform-backup.service','--property=Result','--value']).trim()==='success');
fs.mkdirSync(site,{recursive:true,mode:0o755});for(const [name,expected] of Object.entries(manifest.staticHashes)){const source=path.join(bundle,'site',name),target=path.join(site,name);if(!source.startsWith(path.join(bundle,'site')+path.sep)||hash(fs.readFileSync(source))!==expected)throw new Error(`Static hash mismatch: ${name}`);fs.mkdirSync(path.dirname(target),{recursive:true,mode:0o755});fs.copyFileSync(source,target);fs.chmodSync(target,0o644);}
const migration=fs.readFileSync(path.join(bundle,'migration.sql'),'utf8');if(hash(Buffer.from(migration))!==manifest.migrationHash)throw new Error('Migration hash mismatch');
try{
  run('docker',['exec','-i','lilly-stage-db','psql','-v','ON_ERROR_STOP=1','-U','postgres','-d','postgres'],{input:`begin;\n${migration}\nrollback;\n`,stdio:['pipe','pipe','pipe']});
  run('docker',['exec','-i','lilly-stage-db','psql','-v','ON_ERROR_STOP=1','-U','postgres','-d','postgres'],{input:`begin;\n${migration}\ncommit;\n`,stdio:['pipe','pipe','pipe']});
  check('database enforces aal2',run('docker',['exec','lilly-stage-db','psql','-U','postgres','-d','postgres','-X','-Atc',"select position('aal2' in pg_get_functiondef('public.is_allowed_staff()'::regprocedure))>0"]).trim()==='t');
  switchSite(site);const front=await fetch('https://139.196.97.236/meeting/',{signal:AbortSignal.timeout(15000)}),html=await front.text(),app=await fetch('https://139.196.97.236/meeting/app.js',{signal:AbortSignal.timeout(15000)}),appBytes=Buffer.from(await app.arrayBuffer());
  check('MFA enrollment UI active',front.ok&&html.includes('id="mfaDialog"')&&html.includes('id="mfaQrCode"'));check('MFA gate active before staff session',app.ok&&appBytes.includes(Buffer.from('await requireManagementMfa();await registerStaffSession()')));check('published app hash',hash(appBytes)===manifest.staticHashes['app.js']);
  fs.writeFileSync(path.join(releaseRoot,'ACTIVE'),new Date().toISOString(),{mode:0o600});result.allPassed=true;result.previous=previous;result.site=site;result.finishedAt=new Date().toISOString();fs.writeFileSync(path.join(releaseRoot,'validation.json'),JSON.stringify(result,null,2),{mode:0o600});console.log(JSON.stringify(result));
}catch(error){run('docker',['exec','-i','lilly-stage-db','psql','-v','ON_ERROR_STOP=1','-U','postgres','-d','postgres'],{input:fs.readFileSync(path.join(releaseRoot,'before/is_allowed_staff.sql'),'utf8'),stdio:['pipe','pipe','pipe']});switchSite(previous);fs.writeFileSync(path.join(releaseRoot,'ROLLED_BACK'),String(error?.message||error),{mode:0o600});throw error;}
